type State {
  uuid: String!
  name: String!
  votes: Int!
  chars: Int!
  year: Int!
  election_uuid: String
  sigla: String
  deleted: Boolean!
  deleted_date: Timestamp
  election_quotient: Int!

  coligations: [ Coligation ]
}
